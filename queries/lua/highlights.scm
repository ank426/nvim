;; extends

; Must be done this way as vim isn't captured as any more than a global variable
; (future message) This is the correct way to do it (i think)

((identifier) @namespace.builtin
  (#eq? @namespace.builtin "vim"))
